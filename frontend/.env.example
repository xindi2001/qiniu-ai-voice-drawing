# Backend API root URL for production / preview builds.
# Leave unset during `npm run dev` — Vite proxies /api to localhost:8080.
# Example for local preview: VITE_API_BASE=http://localhost:8080
VITE_API_BASE=
